json_output: true
json_include_output_property: true

stdout_output:
  enabled: true

rules_file:
  - /etc/falco/falco_rules.yaml
  - /etc/falco/rules.d/custom_rules.yaml

plugins:
  - name: container
    library_path: libcontainer.so
    open_params: "unix:///host/var/run/docker.sock"